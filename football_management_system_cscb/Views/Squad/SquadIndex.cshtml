@model IEnumerable<football_management_system_cscb.ViewModels.PlayerViewModel>
@{ ViewData["Title"] = "Squad"; }

<div class="page-header">
    <div>
        <h1>Squad</h1>
        <p class="subtitle">Your current first-team players.</p>
    </div>
</div>

<div class="player-table card">
    <div class="table-responsive scroll-table">
        <table class="table table-hover align-middle mb-0">
            <thead>
                <tr>
                    <th>Player</th>
                    <th>Age</th>
                    <th>Nationality</th>
                    <th>Position</th>
                    <th>Value</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var player in Model)
                {
                    var age = DateTime.Today.Year - player.BirthDate.Year;
          
                    <tr>
                        <td>
                            <strong>@player.FullName</strong>
                            <div class="text-muted small">ID: @player.PlayerId</div>
                        </td>
                        <td>@age</td>
                        <td>@player.Nationality</td>
                        <td><span class="position-badge">@player.PreferredPosition</span></td>
                        <td><span class="rating">@player.Potential</span></td>
                        <td>€@string.Format("{0:N0}", player.MarketValue ?? 0)</td>
                        <td></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
